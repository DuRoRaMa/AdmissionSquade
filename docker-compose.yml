volumes:
  postgres_data:

services:
  pgdb:
    image: postgres:15
    container_name: admission_db
    env_file: ./Admissions_squad/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped